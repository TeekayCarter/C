#include <stdio.h>
#include <conio.h>

int main(void)
{
    char name;

    // name = getch(), //This takes a single character input and jumps to the next line without displaying the output, unless print function is invoked. The This can be used for passwords.

    name = getche(), //This takes a single character input and jumps to the next line displaying the output. 


    printf("");

    // printf("name: %c", name);
    
    return (0);

}
